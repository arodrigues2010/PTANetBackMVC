@model IEnumerable<EsettMvcIntegration.Models.FeeDataModel>

<h2>Fee Data</h2>

<table class="table">
    <thead>
        <tr>
            <th>Timestamp</th>
            <th>Country</th>
            <th>Peak Load Fee</th>
            <th>Weekly Fee</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Timestamp</td>
            <td>@item.Country</td>
            <td>@item.PeakLoadFee</td>
            <td>@item.WeeklyFee</td>
            <td><a href="@Url.Action("Details", new { id = item.Id })">Details</a></td>
        </tr>
    }
    </tbody>
</table>
